apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "investment.fullname" . }}-test-connection"
  labels:
    {{- include "investment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "investment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
